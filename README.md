# JSON Uploader and Formatter

This project allows you to upload a JSON file and format it into a specified i18n format. The formatted JSON data can be downloaded for each mode (e.g., EN.json, AR.json).

